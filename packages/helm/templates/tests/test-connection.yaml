apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gui.fullname" . }}-test-connection"
  labels:
    {{- include "gui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
